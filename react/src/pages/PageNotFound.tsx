import PageNotFoundMessage from "../Ui/PageNotFoundMessage";

function PageNotFound() {
    return (
        <div className="w-full h-[726px] flex items-center justify-center">
            <PageNotFoundMessage />
        </div>
    );
}

export default PageNotFound;
